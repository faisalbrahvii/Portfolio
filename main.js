var typed = new Typed("#text", {
    strings: ['Frontend Developer', 'Collage Student', 'Web Developer'],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
});

AOS.init({
    duration: 1200,
});
